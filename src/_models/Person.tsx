export interface IPerson {
    firstName: string;
    lastName: string;
    dateBorn: string;
}

export enum PersonItem {
    firstName = 'firstName',
    lastName = 'lastName',
    dateBorn = 'dateBorn',
}
