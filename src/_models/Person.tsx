export interface IPerson {
    firstName: string;
    lastName: string;
}
export interface IPersonResult {
    FirstName: string;
    LastName: string;
}

export enum PersonItem {
    firstName = 'FirstName',
    lastName = 'LastName',
}
